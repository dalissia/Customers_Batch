pipeline {
    agent any
    stages {
        stage('Build Back-end') {
            steps {
                // Compile l'application Spring Boot
                sh 'mvn clean install'
            }
        }
        stage('Run Batch (optionnel)') {
            steps {
                 echo 'Job Spring Batch simulé '
            }
        }
    }
}